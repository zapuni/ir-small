# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
# uv keeps the resolved environment in .venv/ — recreate it with `uv sync`.
# The lockfile (uv.lock) and pyproject.toml ARE committed for reproducibility.

# Secrets / local config (the .env holds API keys — do NOT commit it)
.env

# Downloaded embedding model (large; re-download with download_model.py)
src/models/

# Vector embeddings cache (regenerate on demand)
cache/

# OS
Thumbs.db
.DS_Store

# Auto-generated entries
# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Build outputs
dist/
build/
*.log

# Environment files
.env
.env.*
!.env.example

# IDE files
.vscode/
.idea/
*.swp
*.swo

# OS files
.DS_Store
Thumbs.db

# Temporary files
*.tmp
*.temp
.cache/

# Sensitive files (auto-detected)
.env
.env.local

tmp/
data/

